@model IEnumerable<demandeAdmineIdtt.Models.Request>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>Requests List </h3>
<hr/>
<p>
    @Html.ActionLink("+ New Request", "Create",null, new { @class = "btn btn-sm btn-primary" })
</p>
<table class="table table-bordered table-responsive table-hover table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        @if (User.IsInRole("Admin"))
        {
            <th>
                @Html.DisplayName("Employee")
            </th>
        }
        <th>
            @Html.DisplayNameFor(model => model.RequestDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Flag)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedAt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UpdatedAt)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Title)
    </td>
    @if (User.IsInRole("Admin"))
    {
        <td>
            @Html.DisplayFor(modelItem => item.User.UserName)
        </td>
    }    
    <td>
        @Html.DisplayFor(modelItem => item.RequestDate)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Flag)
    </td>
    <td class="@(item.Status == "On Hold" ? "text-warning" : "text-success")">
        @Html.DisplayFor(modelItem => item.Status)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.CreatedAt)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.UpdatedAt)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-sm btn-default" })
        @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-sm btn-info" })

        @if (User.IsInRole("Admin"))
        {
            @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-sm btn-danger" })
        }
        else
        {
            @Html.ActionLink("Remind", "Remind", new { id = item.Id }, new { @class = "btn btn-sm btn-warning" })
        }
    </td>
</tr>
}

</table>
